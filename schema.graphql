type Query {
  currentVersion: String!
  payments: AccountType!
}
schema {
  query: Query
}

union AccountType = Company | Personal

type Company {
  title: String!
  NIP: String!
}

type Personal {
  firstName: String!
  lastName: String!
}
